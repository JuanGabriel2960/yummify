export interface navigationOptionsType {
    text: string;
    icon: string;
    navigate: string;
}

export interface Food{
    calories: number;
    image: string;
    name: string;
    description: string;
    price: number;
}